# @cjsx React.DOM

React = require "react"
pellet = require "pellet"
compo = pellet.components

module.exports = <%- name %>Page = pellet.createClass
  routes: "/<%- name %>"

  #componentConstruction: (options, next) ->
  #  @setProps myData: "loaded from your API"
  #  next()

  #getInitialState: ()->
  #  myData: @props.myData

  render: ->
    <div className="<%- name %>-page">
      <span>My <%- name %> page</span>
    </div>
