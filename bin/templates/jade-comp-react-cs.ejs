React = require "react"
pellet = require "pellet"
compo = pellet.components

<%- name %>View = require "<%- name %>.jade"

module.exports = <%- name %>Component = pellet.createClass
  #getRouteDefaultProps: (ctx, next) ->
  #  ctx.setProps myData: "loaded from your API"
  #  next()

  #getInitialState: ()->
  #  myData: @props.myData

  render: ->
    <%- name %>View
